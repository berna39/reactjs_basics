import react,{ Component } from "react";

function Another(props){
    return(
        <div>
            <p>A functional one man with props { props.property }</p>
        </div>
    );
}

export default Another;